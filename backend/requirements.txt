# Direct dependencies of the FastAPI service only. Previously this file was a
# `pip freeze` of the whole shared .venv, which also pulled in Scrapy/Twisted
# and their transitive deps (see spider/requirements.txt) into the production
# API's install — unnecessary size and attack surface for a service that
# never imports them. Install with:
#   uv pip install -r backend/requirements.txt --python .venv/bin/python
fastapi==0.139.0
uvicorn==0.50.2
SQLAlchemy==2.0.51
PyMySQL==1.2.0
python-dotenv==1.2.2
PyJWT==2.13.0
bcrypt==4.2.1
pydantic==2.13.4
pandas==3.0.3
jieba==0.42.1
# Used by the AI advisor to call the user-configured OpenAI-compatible endpoint
requests==2.34.2
