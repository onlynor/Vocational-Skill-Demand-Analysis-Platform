# 复制为 .env（本地开发）或 .env.production（生产构建）
# cp .env.example .env

# 后端 API 地址。留空则回退到 http://localhost:8000/api（本地 pnpm dev 用）。
# 生产环境按部署方式二选一：
#   - 前端和后端在同一域名下，由 Nginx 反代 /api 到后端 -> VITE_API_BASE_URL=/api
#   - 前后端分开部署 -> VITE_API_BASE_URL=https://api.example.com/api
# VITE_API_BASE_URL=
